import { redirect } from 'next/navigation';import { createClient } from '@/lib/supabase/server';export default async function Agenda(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');return <main className="shell"><section className="dashboardHead"><p className="eyebrow">AGENDA</p><h1>Todo lo que afecta su día</h1><p>Pruebas, tareas, fútbol, controles médicos, cumpleaños y compromisos familiares.</p></section></main>}
