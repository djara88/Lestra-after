import { redirect } from 'next/navigation';import { createClient } from '@/lib/supabase/server';export default async function Alumnos(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');return <main className="shell"><section className="dashboardHead"><p className="eyebrow">ALUMNOS</p><h1>Perfiles de tus hijos</h1><p>El siguiente bloque permitirá crear perfiles con colegio, curso y fecha de nacimiento opcional.</p></section></main>}
