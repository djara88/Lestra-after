import { login } from './actions';
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="loginWrap"><form className="loginCard" action={login}><div className="logo">A</div><p className="eyebrow">LESTRA AFTER</p><h1>Bienvenido</h1><p className="muted">Ingresa para organizar la vida y el estudio de tu familia.</p>{q.error&&<p>No pudimos iniciar sesión. Revisa tus datos.</p>}<label>Correo</label><input name="email" type="email" autoComplete="email" required/><label>Contraseña</label><input name="password" type="password" autoComplete="current-password" required/><button className="primary" type="submit">Entrar</button></form></main>}
