import { Alert, Pressable, SafeAreaView, Text, View } from 'react-native'; import { router } from 'expo-router'; import { supabase } from '@/lib/supabase'; export default function Family(){async function logout(){const {error}=await supabase.auth.signOut();if(error){Alert.alert('Error','No pudimos cerrar la sesión.');return;}router.replace('/login');}return <SafeAreaView style={{flex:1,padding:24,backgroundColor:'#F7F7F5'}}><View><Text style={{fontSize:32,fontWeight:'800'}}>Familia</Text><Text style={{marginTop:8,fontSize:16,color:'#5C626D'}}>Alumnos, responsables, privacidad y preferencias vivirán aquí.</Text><Pressable onPress={logout} style={{marginTop:28,padding:16,backgroundColor:'#111318',borderRadius:16}}><Text style={{color:'#FFF',fontWeight:'800',textAlign:'center'}}>Cerrar sesión</Text></Pressable></View></SafeAreaView>}